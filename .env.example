# Security Settings - CRITICAL: Generate your own values
SECRET_KEY=generate-using-python-secrets-module
DEBUG=False

# Database Configuration
DATABASE_PROFILE=sqlite
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=finance_user
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DATABASE=finance_tracker

# Production Database URL (if using managed database)
# DATABASE_URL=postgresql://user:password@host:port/database

# CORS Settings
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Optional: Rate Limiting
RATE_LIMIT_PER_MINUTE=60

# Optional: File Upload Limits
MAX_FILE_SIZE_MB=10